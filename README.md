# Scheduler: SJF / Shortest CPU Time First
**Author:** Sublet Aurélien  
**Branch:** sjf-shortest-cpu-time-sublet-aurelien

## Summary
This scheduler selects the READY process with the smallest accumulated CPU time:
- selection criterion: `utime + ktime` minimal among READY processes.

## Files provided (FULL FILES)
- `path/to/sched.c`  (replace with the real Nanvix path)

## How to apply
Copy the files into the Nanvix tree, preserving the directory structure.

